export enum ListingType {
    Landing,
    Magazine,
    Bureau,
    Shop,
    MagazineCategory,
    BureauCategory,
    Tag,
    Search
}

export enum NavigationColor {
    White = 'white',
    Black = 'black'
}