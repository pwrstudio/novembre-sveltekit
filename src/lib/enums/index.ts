export enum ListingType {
    Landing,
    Magazine,
    Bureau,
    MagazineCategory,
    BureauCategory,
    Tag,
    Search
}

export enum NavigationColor {
    White = 'white',
    Black = 'black'
}